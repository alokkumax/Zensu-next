import { Search } from 'lucide-react';
import React from 'react';

const SearchBar = () => {
  return (
    <div>
      <Search className="w-5 h-5 hoverEffect text-shop-dark-grey" />
    </div>
  );
}

export default SearchBar;
